create table issues(
                       id VARCHAR(256) NOT NULL PRIMARY KEY,
    summary VARCHAR(256) NOT NULL,
    description VARCHAR(256) NOT NULL
);

create table test(
                       id VARCHAR(256) NOT NULL PRIMARY KEY,
                       summary VARCHAR(256) NOT NULL,
                       description VARCHAR(256) NOT NULL
);